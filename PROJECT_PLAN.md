# CYQUANT 项目计划书

版本 v0.8 · 2026-08-04 · 状态：Phase 0 完成 + 数据服务（存储重构/扫描对齐/WebUI/全历史股票池）就绪，进入 Phase 1 引擎核心

> 阅读指引：总体规划与开发日志见本文件；引擎模块化设计见 ENGINE_DESIGN.md；第一个策略（smart beta Top-N）见 STRATEGY_BLUEPRINT.md。

## 1. 项目定位

- 10 万元起步，A 股沪深主板，日频，全自动（信号生成 → 掘金量化执行，交易全程离手；用户只做数据科学 + 项目管理）。
- 方法论（第一个策略）：横截面收益率预测（smart beta 定位）→ 排序 → Top-N + 约束，long-only 满仓，不做对冲、不做中性化。
- 核心主张：以"回撤预算"为核心约束，在预算内最大化复合收益；用"看板 + 统计预警 + 保险开关"实现进取目标下的风险管理。
- 非目标：不做 T+0/高频；当前不做融资融券与衍生品；不承诺收益。

### 1.1 双主线定位（v0.5 确立）

项目同时承担两条主线，优先级与资产属性不同：

| 主线 | 资产属性 | 目标 |
|---|---|---|
| 平台层（引擎） | 长期工程资产，可复用、可迭代 | 数据、回测、组合构建、风控、绩效全部模块化，接口稳定，供未来所有策略复用 |
| 策略层（应用） | 平台上的第一个应用 | smart beta Top-N 策略跑通并验证平台；研究过程反向驱动引擎迭代 |

- 平台层优先：策略会换，引擎是复利资产。第一个策略只是起点（未来可能有动量择时、行业轮动、多因子复合、regime 切换等）。
- 策略研究可以后置：等引擎与数据基座就绪后再全力研究；研究期间发现的引擎缺口随时进入迭代 backlog，按小版本吸收。
- 接口契约与模块边界必须在早期定义：重构成本随代码量非线性增长，这是本项目最重要的工程任务。

> 说明：第 2–4、8–9 章（收益来源、目标、风控、策略路线）均为**第一个策略**的设计输入；引擎框架保持通用，不绑定策略假设。

## 2. 收益来源定位（smart beta / 预测驱动）【第一个策略】

- 定位：收益来自"准确的横截面收益率预测"（预测溢价 + 市场/风格 beta 的自然暴露），而非对冲后剥离的 alpha。术语上不纠结，含义是：全仓吃 beta + 预测溢价，不追求中性。
- 为什么放弃对冲与组合优化：
  - 对冲工具在 10 万资金下不可用（股指期货门槛 50 万+，个券无做空权限），且对冲浪费小资金的灵活性；
  - 整数/整手约束（100 股/手）使均值-方差类权重解失去意义：10 万 ≈ 15–25 只 × 3000–6000 元/只，5% 权重可能就一两手，优化解取整后误差 10–20%。
  - 因此采用"预测 → 排序 → Top-N + 约束"范式，组合构建用简单规则（等权/加权 + 上限约束 + 整手近似），不用优化器。
- 预测目标变量：未来 h 日**横截面相对收益**（横截面 z-score），而不是绝对收益。模型直接优化 rank IC；市场方向交给仓位/regime 层（可选、后置）。
- 评价体系（数据科学主场）：
  - 主指标：rank IC（日/周/月），含衰减曲线；
  - 辅助指标：hit rate、分层收益单调性、换手率、成本拖累；
  - 绩效报告包含 Grinold 式分解：收益 ≈ 预测精度(IC) × 机会宽度(breadth) × 约束传递(transfer) − 成本损耗，用于定位收益在哪个环节流失。
- 风控含义：由于收益以 beta/风格暴露为主，"风格/行业暴露漂移"与"regime 分类"在预警统计量中权重最高；因子失效监控直接监控预测层的 IC 衰退。

## 3. 目标体系（分层设定）【第一个策略】

| 层级 | 定义 | 作用 |
|---|---|---|
| 底线 | 扣费后跑赢基准（中证500/1000），最大回撤 ≤ 30% | 系统存活线，跌破即硬性降仓 |
| 目标 | 年化 25–40%（视市场状态），回撤 ≤ 25–30% | 设计区间，对应 Sharpe 约 1.2–2.0 |
| 雄心 | 年化 40%+，由牛市 beta 与高预测精度共同贡献 | 研究上限，不设设计约束 |

纪律：
- 回测到实盘通常打 5–8 折（成本、滑点、市场状态变化、执行偏差）。实盘目标 40% ⇒ 回测验收标准定为扣费后样本外年化 60%+ 且稳定。
- 回测里做出 40% 很容易（过拟合/数据窥探），难的是扣费后、样本外仍成立——这是头号工程风险。

## 4. 风险与成本约束

- 复利数学：-20% 需 +25% 回本，-40% 需 +67%。回撤对终值伤害非线性——即使不以夏普为目标，回撤控制也是实现年化的前提。
- 无杠杆下提高风险只有集中度和换手两条路，上限由成本与流动性决定。"追年化"的工程实现 = 在回撤预算内最大化预期复合收益。
- 成本现实（10 万 + 日频）：佣金万2.5（可谈万一/免5）、最低 5 元/笔、印花税卖出 0.05%、过户费万0.1 双边、滑点。日换手 20% 全年摩擦约 7–9%（理想条件），订单碎片化可达 20%+。
- **成本模型是回测引擎的一等公民**——这是引擎平台的通用能力，不是某个策略的特设。

## 5. 总体架构（平台 / 策略分离）

```text
平台层（可复用资产）
┌──────────────────────────────────────────────────────┐
│ core/ 接口契约  ←  data/ 数据基座                     │
│ backtest/ 回测引擎 ← portfolio/ 组合构建 ← risk/ 风控  │
│ 绩效分析（收益/回撤/换手/Grinold 分解）                │
└──────────────────────────────────────────────────────┘
策略层（第一个应用）
factors/ + model/ → 实现 Strategy 接口 → 注册进引擎
执行层
execution/gm_strategy.py：读信号 → 风控检查 → 限价单 → 实盘
```

- 依赖方向单向：策略层只依赖平台层接口，平台层不感知具体策略。
- 未来换平台只替换执行适配器；未来加策略只新增 Strategy 实现与配置。
- 模块化设计与接口契约详见 ENGINE_DESIGN.md。

## 6. 回测体系决策

- 决策：自研轻量事件驱动回测引擎为主；掘金回测做交叉验证；qlib 暂不引入主流程（后续可作 ML 研究层组件）。
- 理由：A 股细节（T+1、涨跌停、停牌、除权除息、佣金最低 5 元、印花税、滑点、整手 100 股）必须透明可控；掘金回测黑盒且扩展受限；qlib 重、数据接入复杂、维护成本高。
- 自研引擎必须覆盖：日频 bar、T+1、涨跌停不可成交、停牌跳过、整手约束、成本模型、组合约束、风险乘数、绩效与 Grinold 分解。
- 引擎版本化：回测引擎作为平台模块语义化演进（见第 11 章），第一个稳定版在 Phase 3 前后冻结。
- 验证：同一策略在自研引擎与掘金回测上 A/B 对比，偏差需在可解释范围内。

## 7. 数据与股票池

- 数据源：掘金量化数据接口（行情、基本面、复权、停牌、涨跌停）。
- 数据层股票池（v0.8）：沪深全 A 股（主板/创业板/科创板，含全部历史退市股，约 5542 只），供行情缓存与截面对齐，避免幸存者偏差；北交所/B 股不取。
- 策略层股票池：沪深主板（排除创业板/科创板/北交所/退市股）；剔除 ST/*ST、上市 <60 日、长期停牌、流动性后 10%。
- 复权与事件：回测用不复权 + 除权除息事件，或后复权；涨跌停、停牌信息进入引擎。

## 8. 第一个策略：方法路线（v1.0）【策略层】

### 8.1 特征与模型
- 特征族：动量/反转、质量、低波、估值、资金面/流动性、量价微观结构（每族 2–3 个候选）。
- 模型：滚动训练（LightGBM 等），目标为未来 h 日横截面相对收益，直接优化 rank IC；v1.0 可先线性/打分合成，v1.1 上 ML。
- 组合：Top 20–30 只，个股上限、行业上限、换手上限、整手近似、涨跌停不可成交。
- 验证：滚动样本外、rank IC 与衰减、分层回测、参数扰动、成本敏感性、regime 分状态检验。

### 8.2 组合构建进阶（风险预算化 Top-N）

- 问题：固定行业权重上限会错过主线明确时的机会；无约束又会导致行业集中、放大风险。两者矛盾的根源是把"行业仓位"当成了风险度量的代理。
- 方案一（先落地，Phase 2 基准版）：固定行业权重上限（如 20%），先把 pipeline 跑通。
- 方案二（升级）：把"行业仓位上限"改为"行业风险贡献上限"：
  - 个股风险贡献 RC_i = w_i × (Σw)_i / σ_p²，行业风险贡献 = 该行业内个股 RC 之和；
  - 协方差用所选 20–30 只股票的收益率估计（Ledoit-Wolf 收缩，或行业/风格因子模型，后者更稳）；
  - 实现不依赖优化器：打分加权 + 迭代/贪心调整权重直至满足风险预算约束。
- 方案三（信号条件化）：行业风险预算 = 基础预算 + f(信号强度)。信号强度候选统计量：Top 组合平均 z-score、预测 spread（Top vs Bottom 预测差）、滚动 IC 置信度。信号弱时收紧（如 ≤15%），强时放宽（≤35%），保留硬性地板（≤40–50%）作为生存线。
- 已知风险与对策：
  - 条件化预算会放大模型误差——扩张规则必须用实现信号质量（预测 spread 与实现 IC 的偏差、hit rate）校准，不能只用预测值本身；
  - 协方差估计噪声大，需收缩/因子模型；
  - 整手取整后风险贡献会偏移，必须取整后重算校验（允许小容差）。

## 9. 风险管理架构（看板 + 统计预警 + 保险开关）【风控框架作为平台通用模块实现，与策略解耦】

第一层 硬性自动风控（默认锁定，改需双重确认）：
- 单日亏损熔断、最大回撤硬线、单票/行业/风格暴露上限、涨停不追/跌停排队不卖；
- 策略参数（因子权重/模型参数）实盘期默认锁定。

第二层 统计预警（回撤还小时触发，核心）：
- 滚动因子/模型 rank IC 衰退监控（EWMA 或滚动窗口），偏离历史分布 → 预测能力失效预警；
- 策略超额收益 CUSUM / EWMA 控制图（均值漂移检测）；
- 已实现波动率 vs 预期波动率突变（方差比检验）→ 自动下调风险乘数；
- 尾部风险：滚动 VaR/CVaR、极值阈值、亏损分布偏度；
- 风格/行业暴露漂移（组合 vs 基准/历史）；
- 市场状态分类器（趋势/震荡/风险）概率输出，regime 切换预警；
- 执行质量：实际滑点 vs 预期、成交率、换手偏差。

第三层 人工介入（平时自动，关键可调）：
- 风险预算参数（风险乘数 0–1、目标仓位）可调；策略参数默认不可调；
- 每次手调强制记录：时间、操作人、原因、调整前后、预期影响，24h 内自动对比；
- 熔断开关（kill switch）仅用户本人可触发，触发后进入"只减仓不新增"模式；
- 铁律：预警剧本与手调流程必须先在建仓前/模拟盘演练。

## 10. 参数管理原则

- 全部参数进 config/（数据类或 YAML），机制（预警阈值、熔断动作）也建模为配置选项；代码不写死数字。
- 参数分两类：
  - 结构参数（机制）：架构阶段定，如熔断动作、预警统计量类型；
  - 标定参数（数值）：开发阶段用数据确定，如持仓集中度、回撤硬线、风险容忍。
- 规则：修改任何参数 → 自动重跑验证套件（回归测试 + 关键回测），防止越界参数悄悄改变风险画像。
- 待定标定参数清单（开发阶段逐项标定）：持仓数/个股上限/行业上限/换手上限；回撤硬线与熔断阈值；风险乘数映射；预警控制限；regime 阈值；成本参数；行业风险贡献预算与信号强度函数；预测窗口 h、Top-N 数量。

## 11. 版本管理策略（v0.5 新增）

- 平台（引擎）版本：语义化版本 v0.1.x（接口可演进）→ v1.0.0（接口冻结，向后兼容承诺）→ v1.x（兼容新增）。
- 策略版本独立：v1.0 = smart beta Top-N；策略版本不影响引擎版本。
- 迭代记录：引擎变更记入 ENGINE_DESIGN.md 与 develop.md；策略变更记入 STRATEGY_BLUEPRINT.md。
- 版本推进前，todo.md 对应待办须全部完成并总结进 develop.md。

## 12. 路线图与验收标准（v0.5 重排）

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| Phase 0 工程与数据基座（约 2 周） | 骨架与 config、core 接口契约草案、掘金 SDK、股票池、数据缓存 | 骨架可运行；数据每日自动更新；抽样核对正确 |
| Phase 1 引擎核心 v0.1（2–3 周） | 事件驱动回测最小可用（日频/T+1/涨跌停/停牌/整手/成本模型）+ 组合构建框架（Top-N builder）+ 风控框架（硬风控/风险乘数）+ 绩效基础 | 引擎单测通过；与掘金回测 A/B 偏差可解释；成本敏感性报告 |
| Phase 2 数据完善 + 引擎 v0.2（2 周） | 复权/除权除息核对、因子工具库、绩效扩展（Grinold 分解、IC 分析）、预警统计量框架 | 数据管道稳定；绩效报告完整 |
| Phase 3 第一个策略 v1.0（3–4 周） | 因子实验室（rank IC/分层/衰减）、预测模型（线性→ML）、策略实现与 walk-forward、风险预算标定 | 扣费后样本外年化 60%+、回撤 ≤25%、rank IC 稳定；研究驱动的引擎缺口全部进入迭代闭环 |
| Phase 4 模拟盘（3–6 个月） | 掘金模拟、信号流水线自动化、看板与预警上线、剧本演练 | 模拟收益与回测偏差可解释；预警全部演练通过 |
| Phase 5 实盘上线 | 10 万实盘、保守起步、每周复盘 | 运行平稳；手调流程已演练；熔断机制生效 |

- 引擎版本推进：Phase 1 → v0.1；Phase 2 → v0.2；Phase 3 结束视稳定度决定是否冻结 v1.0。
- 策略研究与引擎迭代允许并行：研究发现的引擎缺口进入 backlog，按小版本迭代吸收，不阻塞策略主线。

工具解锁路线（资本增长后的可选升级）：50 万 → 两融/科创板/北交所；后续如需对冲再评估股指期货/期权。

## 13. 目录结构

```text
cyquant/
├── PROJECT_PLAN.md / ENGINE_DESIGN.md / STRATEGY_BLUEPRINT.md
├── develop.md / todo.md / OUTLINE.md / AGENTS.md
├── config/          # 全局参数（成本、股票池、风控、预警阈值）——所有数字的所在地
├── core/            # 接口契约与领域模型（Strategy/DataSource/CostModel/RiskManager…）
├── data/            # 数据缓存与数据脚本（DataHub）
├── factors/         # 特征计算与单特征检验（策略层工具）
├── model/           # 收益率预测模型与滚动训练（策略层工具）
├── portfolio/       # 组合构建框架（Top-N builder、约束、整手处理）
├── backtest/        # 事件驱动回测引擎、绩效分析、Grinold 分解
├── risk/            # 监控统计量、预警、熔断、风险乘数
├── research/        # notebook 研究
└── execution/       # 掘金策略（读信号、风控、下单）
```

## 14. 开发日志（最新在前）

### v0.8（2026-08-04）数据服务修正：全历史股票池 + 退市股结论推翻
- 推翻此前"掘金缺 2019–2024 退市股"结论：get_symbols 全量 5542 只含全部历史退市股（2002 PT金田A → 2026 退市潮），元数据/行情/停牌状态全部可查，历史行情与停牌日严格对齐；股票池接口 get_instruments → get_symbols。
- 策略静态池补"未退市"过滤；WebUI 文案纠正；全量/增量任务判断与对齐逻辑经核实无遗漏（详见 develop.md）。
- 首次对齐需在旧池任务完成后用新池重跑补齐退市股；已抓数据保留，只补缺失。

### v0.7（2026-08-04）数据服务：存储重构 + WebUI
- 存储布局升级为「资产类别/年份」分区 + 覆盖清单；旧缓存 541 万行迁移完成并安全删除（回收站）。
- 抓取模式区分：市场扫描=按股票逐个对齐（股票维度）；截面增量=按交易日对齐（日期维度）；均先扫描本地、只补缺失。
- WebUI（Streamlit + APScheduler）上线：按钮触发任务、动态进度条、未对齐统计、工作日定时调度。
- 掘金官方限流规则已查证并记录（行情接口不受分钟/每日表限制，单次 16MB 为实际约束）。
- 详见 develop.md；数据抓取由用户在 WebUI 手动触发。

### Phase 0（2026-08-04）工程与数据基座完成
- 环境与骨架：Python 3.10.6 venv、git init、11 目录、pyproject.toml、.gitignore；pytest 5 用例通过。
- core 接口契约与领域模型落地；config 五个 YAML + loader 校验；滑点占位、免5 待确认。
- 掘金 SDK 接入验证；解决 gm 单次 16MB 上限（50 只/批 × 1 年分段拉取）。
- 股票池构建：静态过滤 2990 只 → 流动性过滤后 2737 只；2737 只 × 2018 至今日线 525 万行缓存；抽样核对通过。
- 详见 develop.md；下一迭代：Phase 1 引擎核心 v0.1。

### v0.5（2026-08-04）主线调整：引擎平台优先
- 项目定位升级为双主线：平台层（可复用、可迭代的引擎）+ 策略层（第一个应用 smart beta Top-N）；平台优先，策略研究后置/并行。
- 新增 ENGINE_DESIGN.md：模块地图、接口契约草案、版本管理、测试与验证、项目管理流程。
- 路线图重排：因子实验室与预测模型从 Phase 1 后移至 Phase 3；引擎核心（回测/组合/风控）提前至 Phase 1–2。
- 文档修正：develop.md 日志顺序整理为最新在前；OUTLINE.md 补齐为项目总纲；版本号同步。

### v0.4（2026-08-03）组合构建：风险预算化 Top-N
- 提出并讨论"固定行业权重上限 vs 主线集中"的矛盾。
- 结论：将"行业仓位上限"升级为"行业风险贡献上限"（RC_i = w_i × (Σw)_i / σ_p²），并进一步升级为信号强度条件化的状态依赖风险预算；保留硬性风险地板。
- 实现路径：固定上限基准版（Phase 2）→ 风险贡献版（Phase 2.5/3）→ 信号条件化版（Phase 3）；整手取整后重算校验。
- 关键纪律：条件化预算的扩张规则必须用实现信号质量校准，防止模型过度自信时放大错误集中。

### v0.3（2026-08-03）收益来源定位与参数管理原则
- 收益来源定位为横截面收益率预测（smart beta）：long-only 满仓、不对冲、不中性化；整数约束使组合优化失效，采用排序 Top-N + 约束。
- 预测目标变量定为横截面相对收益（z-score），评价以 rank IC 为核心，绩效含 Grinold 式分解。
- 参数管理原则：全部参数进 config，结构参数现在定、标定参数开发阶段定；改参数自动重跑验证套件。

### v0.2（2026-08-03）目标与风控架构修订
- 目标体系分层（底线/目标 25–40%/雄心 40%+）；"追年化不追夏普"落地为回撤预算内最大化复合收益；回测验收目标上修至 60%+。
- 风控三层架构确定：硬性自动风控 / 统计预警（IC 衰退、CUSUM、波动率突变、VaR/CVaR、regime）/ 人工介入协议。

### v0.1（2026-08-03）立项
- 定位：10 万、主板、日频、全自动（掘金执行）。
- 回测方案：自研引擎为主、掘金交叉验证、qlib 暂缓。
- 关键风险：小资金摩擦成本、主板权限限制、掘金回测扩展性。
