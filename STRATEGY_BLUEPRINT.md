# 策略蓝图（Strategy Blueprint）

版本 v1.1 · 2026-08-04 · 与 PROJECT_PLAN.md 配套

> 本文档专注**第一个策略**（smart beta Top-N）开发本身（收益来源、预测模型、组合构建、风控、验证）。
> 平台层（引擎）的模块边界与接口契约见 ENGINE_DESIGN.md；总体规划与数据工程见 PROJECT_PLAN.md。
> v1.1 修订（2026-08-04）：明确本文档定位为"平台上的第一个策略"；策略内容无实质变化，仅文档分层。

---

## 1. 策略边界与约束（讨论定稿）

- 资金：10 万元起步；A 股沪深主板（无创业板/科创板/北交所权限）。
- 频率：日频调仓；交易全程自动离手（用户只做数据科学）。
- 多空约束：long-only、满仓，无杠杆、无做空、无对冲工具。
- 股票池约束：沪深主板；剔除 ST/*ST、上市不足 60 日、长期停牌、流动性后 10%。
- 交易摩擦现实（10 万 + 日频）：
  - 佣金万2.5（可谈万一、免5），普遍最低 5 元/笔——5000 元订单按 5 元收就是单边 0.1%；
  - 印花税卖出 0.05%，过户费万0.1 双边，滑点视流动性；
  - 日换手 20% 时全年摩擦成本约 7–9%（理想条件），订单碎片化可到 20%+。
  - 结论：成本模型是策略验证的一等公民；组合要控制换手；持仓 15–30 只、避免碎片化订单。

## 2. 收益来源定位：横截面收益率预测（smart beta）

### 核心决策
收益来源 = **准确的横截面收益率预测**，而非对冲后剥离的 alpha。
全仓吃市场/风格 beta + 预测溢价，不做中性化、不做对冲。

### 为什么（讨论中的关键理由）
1. **小资金对冲浪费灵活性**：股指期货 50 万门槛、个券无做空权限；对冲成本对小资金不划算。
2. **整数/整手约束使均值-方差优化失去意义**：10 万 ≈ 15–25 只 × 3000–6000 元/只，5% 权重可能就一两手（100 股/手）；优化权重取整到整手后误差 10–20%，"最优解"是自欺。
3. 因此组合构建采用 **"预测 → 排序 → Top-N + 上限约束 + 整手近似"** 范式，不用优化器。

### 预测目标变量
- 未来 h 日**横截面相对收益**（横截面 z-score），而非绝对收益。
- 绝对收益被市场 beta 主导、预测难度高，且与 long-only 定位错位；横截面排名是数据科学的主场，也是 rank IC 的直接优化对象。
- 市场方向交给仓位/regime 层（可选、后置），不做端到端绝对收益预测。

### 评价体系
- 主指标：**rank IC**（日/周/月），含衰减曲线——预测排名与未来实际收益排名的相关。
- 辅助指标：hit rate、分层收益单调性、换手率、成本拖累。
- Grinold 式分解（绩效报告必备）：收益 ≈ 预测精度(IC) × 机会宽度(breadth) × 约束传递(transfer) − 成本损耗，用于定位收益在哪个环节流失。

### 对风控的含义
收益以 beta/风格暴露为主 ⇒ "风格/行业暴露漂移"与"regime 分类"在预警统计量中权重最高；
"因子失效"预警 = 直接监控预测层的 rank IC 衰退，而不是净值回撤。

## 3. 目标体系（分层设定）

| 层级 | 定义 | 作用 |
|---|---|---|
| 底线 | 扣费后跑赢基准（中证500/1000），最大回撤 ≤ 30% | 系统存活线，跌破即硬性降仓 |
| 目标 | 年化 25–40%（视市场状态），回撤 ≤ 25–30% | 设计区间，对应 Sharpe 约 1.2–2.0 |
| 雄心 | 年化 40%+，由牛市 beta 与高预测精度共同贡献 | 研究上限，不设设计约束 |

### 关键纪律（讨论校准）
- **回测打折规则**：回测到实盘通常打 5–8 折（成本、滑点、市场状态变化、执行偏差）。实盘目标 40% ⇒ 回测验收标准 = 扣费后样本外年化 60%+ 且稳定。
- **回测里做出 40% 很容易**（过拟合/数据窥探），难的是扣费后、样本外仍成立——这是头号工程风险。
- **复利数学**：-20% 需 +25% 回本，-40% 需 +67%；回撤对终值伤害非线性。即使不以夏普为目标，回撤控制也是实现年化的前提。
- **"追年化不追夏普"的工程含义**：无杠杆下提高风险只有集中度和换手两条路，上限由成本与流动性决定。因此落地为"在最大可接受回撤预算内最大化预期复合收益"（drawdown-constrained growth），而非"不管风险"。
- 震荡市一年翻倍 ≈ 需要 Sharpe 3+（全球顶级水准），不可作为设计目标；牛市翻倍主要由市场 beta 贡献，无需为它设计。

## 4. 预测模型路线（v1.0 → v1.1）

### 特征族（候选，每族 2–3 个）
- 动量/反转（短中长期）
- 质量（ROE/毛利率/现金流稳定性等）
- 低波（历史波动率、beta）
- 估值（PE/PB/股息率等，需注意行业可比性）
- 资金面/流动性（换手率、成交额变化、北向/大单行为若可得）
- 量价微观结构（短期收益率形态、波动聚集、量价背离等）

### 模型演进
- v1.0：线性打分/加权合成起步（可解释、稳健、快速跑通 pipeline）。
- v1.1：机器学习增强——滚动训练（LightGBM 等），目标为横截面相对收益，直接优化 rank IC；
  滚动重训、样本外验证、特征重要性监控。

### 验证要求
滚动样本外、rank IC 与衰减、分层回测、参数扰动、成本敏感性、regime 分状态检验。

## 5. 组合构建：风险预算化 Top-N（核心设计）

### 5.1 问题（讨论起点）
排序 + Top-N + 固定行业上限容易错过主线明确时的机会；无约束又导致行业集中、放大风险。
两者矛盾的根源：把"行业仓位"当成了风险度量的代理。

### 5.2 方案（三台阶）

| 方案 | 内容 | 阶段 |
|---|---|---|
| 一：基准版 | 固定行业权重上限（如 20%），排序 Top-N + 整手 | Phase 2 先落地，跑通 pipeline |
| 二：风险贡献版 | "行业仓位上限" → "行业风险贡献上限" | Phase 2.5/3 |
| 三：信号条件化版 | 行业风险预算 = 基础预算 + f(信号强度)，"超额够高就允许赌主线" | Phase 3 |

### 5.3 数学定义
- 个股风险贡献：RC_i = w_i × (Σw)_i / σ_p²，其中 Σ 为组合协方差、σ_p² = wᵀΣw；
- 行业风险贡献 = 该行业内个股 RC 之和；
- 约束：行业风险贡献 ≤ 预算；
- 状态依赖预算：预算 = 基础值 + f(信号强度)，信号强度候选统计量：Top 组合平均 z-score、预测 spread（Top vs Bottom 预测差）、滚动 IC 置信度；
- 硬性地板：单行业风险贡献 ≤ 40–50%（生存线，不允许放开）；
- 整手取整后必须重算风险贡献并校验（允许小容差）。

### 5.4 实现要点
- 组合层面只需所选 20–30 只股票的协方差（20–30 维），计算量极小；
- 协方差估计用 Ledoit-Wolf 收缩，或行业/风格因子模型（更稳、更可解释，可直接喂给暴露漂移预警）；
- 不依赖优化器：打分加权 + 迭代/贪心调整权重直至满足风险预算约束。

### 5.5 三个坑与对策（重点记录）
1. **条件化预算会放大模型误差（最危险）**：模型过度自信于某主线时（常与过拟合同时发生）会把预算放给错误方向——在最不该集中的时候集中。
   → 对策：扩张规则必须用**实现信号质量**校准（预测 spread 与实现 IC 的偏差、hit rate），信号被验证过才给预算，不能只用预测值驱动。
2. **协方差估计噪声大**：60–120 日样本协方差小样本下极不稳，不用收缩/因子模型则"量化风险"失真。
3. **整手取整破坏风险贡献**：取整到 100 股后必须重算风险指标，超限微调，不能把连续解直接当成交清单。

### 5.6 术语参考
risk budgeting / 风险预算；Black-Litterman 观点倾斜；状态依赖风险预算（state-dependent risk budget）。

## 6. 风险管理架构（看板 + 统计预警 + 保险开关）

### 第一层：硬性自动风控（默认锁定，改需双重确认）
- 单日亏损熔断（如 -5% → 次日风险乘数降至 0.5）；
- 最大回撤硬线（25% → 半仓，35% → 清仓复盘）；
- 单票/行业/风格暴露上限；涨停不追、跌停排队不卖；
- 策略参数（因子权重/模型参数）实盘期默认锁定，禁止手调。

### 第二层：统计预警（回撤还小时触发，核心）
- 滚动模型/因子 rank IC 衰退监控（EWMA 或滚动窗口），偏离历史分布 → 预测能力失效预警；
- 策略超额收益 CUSUM / EWMA 控制图（均值漂移检测）；
- 已实现波动率 vs 预期波动率突变（方差比检验）→ 自动下调风险乘数；
- 尾部风险：滚动 VaR/CVaR、极值阈值、亏损分布偏度；
- 风格/行业暴露漂移（组合 vs 基准/历史）——收益主要来自 beta 时的最高权重预警；
- 市场状态分类器（趋势/震荡/风险）概率输出，regime 切换预警；
- 执行质量：实际滑点 vs 预期、成交率、换手偏差。

### 第三层：人工介入（平时自动，关键可调）
- 风险预算参数（风险乘数 0–1、目标仓位）可调；策略参数默认不可调；
- 每次手调强制记录：时间、操作人、原因、调整前后、预期影响，24h 内自动对比；
- 熔断开关（kill switch）仅用户本人可触发，触发后进入"只减仓不新增"模式；
- 铁律：预警剧本与手调流程必须先在建仓前/模拟盘演练，实盘首次触发时不允许是第一次思考怎么办。

## 7. 回测体系（策略验证视角）

- 决策：**自研轻量事件驱动回测引擎为主**；掘金回测做交叉验证；qlib 暂不引入主流程（后续可作 ML 研究层组件）。
- 理由：A 股细节必须透明可控；掘金回测黑盒且扩展受限；qlib 重、数据接入复杂、维护成本高。
- 引擎必须覆盖：日频 bar、T+1、涨跌停不可成交、停牌跳过、整手 100 股、成本模型（佣金/免5、印花税、过户费、滑点）、组合约束、风险乘数、绩效与 Grinold 分解。
- 验证：同一策略在自研引擎与掘金回测 A/B 对比，偏差需在可解释范围内；成本敏感性报告；walk-forward；regime 分状态检验。

## 8. 参数管理原则

- 全部参数进 config（数据类或 YAML），机制（预警阈值、熔断动作）也建模为配置选项；代码不写死数字。
- 参数分两类：
  - 结构参数（机制）：架构阶段定，如熔断动作、预警统计量类型；
  - 标定参数（数值）：开发阶段用数据确定。
- 规则：修改任何参数 → 自动重跑验证套件（回归测试 + 关键回测），防止越界参数悄悄改变风险画像。
- **"pipeline 写通后改参数就只是改数字"——成立的前提是以上两条纪律。**

### 待定标定参数清单（开发阶段逐项标定，现在只需记住存在）
- 持仓数 / 个股上限 / 行业上限 / 换手上限；
- 最大回撤硬线与半仓/清仓阈值、单日亏损熔断阈值；
- 风险乘数标定（回撤与波动率 → 风险乘数映射）；
- 行业风险贡献预算（基础值/放宽值/硬地板）与信号强度函数定义、阈值；
- 预警统计量控制限（EWMA/CUSUM 灵敏度、IC 偏离阈值、VaR 置信水平）；
- regime 分类器阈值与切换响应；
- 成本模型参数（滑点、佣金、免5与否）；
- 预测窗口 h、Top-N 数量。

## 9. 策略开发路线与验收标准

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| Phase 1 预测与特征实验室（2–3 周） | 特征库 + rank IC/ICIR/分层检验，确定预测目标 h | 单特征检验报告；明确入选特征与 h 窗口 |
| Phase 2 组合与回测引擎 v1（2–3 周） | 自研引擎 + 排序 Top-N + 固定上限版约束 + 整手 + 风险乘数 + Grinold 分解 | 与掘金回测 A/B 偏差可解释；成本敏感性报告 |
| Phase 3 策略 v1.0（2–3 周） | 预测模型、滚动训练、walk-forward、预警统计量实现、风险贡献预算与信号条件化标定 | 扣费后样本外年化 60%+、回撤 ≤25%、rank IC 稳定；风险预算版 vs 固定上限版 A/B 对比 |
| Phase 4 模拟盘（3–6 个月） | 信号流水线自动化、看板与预警上线、剧本演练 | 模拟收益与回测偏差可解释；预警全部演练通过 |
| Phase 5 实盘上线 | 保守起步、每周复盘 | 运行平稳；手调流程已演练；熔断机制生效 |

## 10. 关键讨论记录（决策日志）

### 2026-08-03 · 初始方向
- 讨论：打分选股 vs 标准多因子；散户缺资金体量和工具，如何获得更高超额。
- 结论：打分选股本质就是多因子散户版；散户的真实优势是容量自由（小资金可做小市值）、换手自由、不做对冲；必须配套严格的成本模型与风格/行业约束。
- 关键警告：简单选股的系统性风险不是选股模型，而是风格暴露、数据偏差（幸存者/前视）、过拟合、成本吞噬。

### 2026-08-03 · 目标校准
- 讨论："震荡市一年翻倍不算高"的预期。
- 结论：翻倍 ≈ 年化 100%、Sharpe 3+，属世界顶级水准，不可作为设计目标；分层目标体系（底线/目标 25–40%/雄心 40%+）；回测验收目标上修至 60%+。

### 2026-08-03 · 追年化不追夏普
- 讨论：用户主要方向是追年化，用看板 + 统计预警 + 保险开关管理，平时自动、关键时允许手调。
- 结论：落地为"回撤预算内最大化复合收益"；三层风控架构；人工介入必须协议化（参数分类、记录、熔断、剧本演练）。

### 2026-08-03 · 收益来源定位（smart beta）
- 讨论：收益来源是准确的收益率预测而非 alpha；小资金对冲浪费灵活性；整数约束影响太大。
- 结论：long-only 满仓、不对冲；预测横截面相对收益（z-score）；rank IC 为第一等指标；组合构建用排序 Top-N + 约束，不用优化器。

### 2026-08-03 · 回测引擎选型
- 讨论：掘金回测局限，是否用 qlib 或自研。
- 结论：自研轻量事件驱动引擎为主（A 股细节透明可控），掘金回测交叉验证，qlib 暂缓。

### 2026-08-03 · 风险预算化 Top-N
- 讨论：固定行业上限错过主线 vs 无约束行业集中；想法"超额收益够高就允许赌主线"。
- 结论：约束从"行业仓位上限"升级为"行业风险贡献上限"，再升级为信号强度条件化的状态依赖风险预算；保留硬性风险地板；扩张规则必须用实现信号质量校准。

### 2026-08-03 · 参数管理
- 讨论：具体参数（集中度、风险容忍）现在要不要定。
- 结论：机制现在定、数值开发阶段标定；全部参数进 config；改参数自动重跑验证套件；待定清单已记录。

## 11. 开发阶段待解决项（暂不阻塞，记录在案）

- 预测窗口 h 的选择（候选 5/10/20 日）；
- 信号强度函数的具体形式（平均 z-score / 预测 spread / IC 置信度，或组合）；
- 风险预算数值（基础/放宽/硬地板）与回撤硬线、熔断阈值；
- 特征族的最终入选与模型形态（线性起步还是直接 ML）；
- Top-N 数量（15–30 区间内标定）。
